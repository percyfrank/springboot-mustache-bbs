server:
  servlet:
    encoding:
      force-response: true

spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/like_lion
    username: root
    password: osoh140109@!

  # ??? SQL ?? ??
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    # hibernate ??
    database : mysql
    hibernate.ddl-auto : validate
